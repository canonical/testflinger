name: Create version tag for agent host terraform module
permissions:
  contents: read
on:
  push:
    branches:
      - main
    paths:
      - agent/terraform/**

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  auto-tag-terraform-module:
    name: Create version tag for the terraform module
    runs-on: ubuntu-latest
    permissions:
      contents: write  # necessary for writing tag

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0  # necessary for semantic versioning
          filter: blob:none # exclude file contents for faster checkout
          persist-credentials: true  # necessary for pushing tags

      - name: Generate semantic version
        id: version
        uses: PaulHatch/semantic-version@4d6da96a9bc3116da3613309a11ff75fba58eb8d  # v6.0.4
        with:
          tag_prefix: "testflinger-agent-host-"
          major_pattern: "breaking(terraform-agent):"
          minor_pattern: "feat(terraform-agent):"
          change_path: "agent/terraform/"
          search_commit_body: true
          enable_prerelease_mode: false

      - name: Create and push tag
        env:
          TAG: ${{ steps.version.outputs.version_tag }}
        run: |          
          echo ::group::Create tag
          git tag "$TAG"
          echo ::endgroup::
          echo ::notice::"$TAG"
          echo ::group::Push tag
          git push origin $TAG
          echo ::endgroup::
