import logging

logger = logging.getLogger()

fw1, fw2, min_req_ver = "1", "2", "3"

logger.error(
    "[%s] current firmware %s doesn't meet"
    "minimum active version %s"
    % (
        fw1,
        fw2,
        min_req_ver,
    )
)
