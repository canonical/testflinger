name: ai
base: ubuntu@24.04
sdks:
  - name: opencode
  - name: claude-code
  - name: just
  - name: testflinger-mcp
  - name: hexr-mcp

actions:
  opencode: opencode "$@"
  claude: claude "$@"
  just: just "$@"
